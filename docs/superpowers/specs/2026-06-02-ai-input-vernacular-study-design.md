# AI Input Vernacular Study — Design Spec
_Date: 2026-06-02_
_Updated: 2026-06-02 — LLM execution model changed from direct Anthropic SDK calls to Claude Code Agent orchestration_

## Goal

Determine whether abbreviated or informal input vernacular (texting shorthand, word truncation) can maintain AI output quality while reducing input token usage. The study compares three levels of input formality against MMLU benchmark questions with known correct answers, tracking accuracy and token usage at each level.

---

## Study Design

### Input Levels

Every question is tested at three levels. The only variable between levels is the question text itself — all other inputs are held constant.

| Level | Description | Example |
|-------|-------------|---------|
| L1 | Normal spelling and grammar (control) | "What are the primary causes of inflation?" |
| L2 | Texting-style abbreviations only | "Wut r d primary causes of inflation?" |
| L3 | L2 abbreviations + further word truncation via LLM | "Wut r d prmry causes of infltn?" |

### Benchmark

- **Dataset:** MMLU (Massive Multitask Language Understanding) via Hugging Face `datasets`
- **Split:** test
- **Sample:** 2 questions per subject × 57 subjects = **114 questions**
- **Total Agent calls (evaluation):** 114 × 3 levels = **342 calls**

---

## Execution Model

**LLM calls are made via Claude Code's Agent infrastructure**, not via direct Anthropic API calls. No `ANTHROPIC_API_KEY` is needed. This means:

- **Phase 1 (L3 generation)** is triggered by asking Claude Code to run the Phase 1 workflow. Claude Code spawns 114 Agent calls in parallel, collects the L3 texts, and writes `data/question_set.json`.
- **Phase 2 (evaluation)** is triggered by asking Claude Code to run the Phase 2 workflow. Claude Code spawns 342 isolated Agent calls and writes the results CSV/XLSX/report via Python utilities.
- The evaluation model is specified as a workflow argument (e.g. `claude-sonnet-4-6`); the L3-generation model is always `claude-haiku-4-5-20251001`.

Python scripts are **data utilities only** — they handle MMLU loading, L2 transformation, JSON I/O, and report generation. They make no LLM calls.

---

## Project Structure

```
/
├── data/
│   ├── raw_questions.json        # intermediate: L1+L2 only, generated by Python script
│   └── question_set.json         # final: L1+L2+L3, generated by Phase 1 workflow, committed to repo
├── results/
│   ├── raw_results_<model>.csv   # one row per Agent call (342 rows)
│   ├── raw_results_<model>.xlsx  # same data, Excel format
│   └── summary_report_<model>.md # auto-generated findings
├── workflows/
│   ├── phase1_l3_generation.js   # Claude Code workflow: generates L3 variants via agents
│   └── phase2_evaluation.js      # Claude Code workflow: evaluates all 342 question/level pairs
├── phase1_generate_questions.py  # data utility: MMLU loading, L2 transform, JSON I/O
├── phase2_evaluate.py            # data utility: message formatting, result building, JSON I/O
├── abbreviations.py              # L2 lookup table
├── report.py                     # CSV/XLSX writing, summary report generation
└── requirements.txt
```

`question_set.json` is a committed artifact — it locks in the exact questions and all three variants, making the study fully reproducible by anyone who clones the repo (they can run Phase 2 without re-running Phase 1).

---

## Phase 1: Question Set Generation

**Triggered by:** Asking Claude Code to run the Phase 1 workflow.
**Output committed to repo:** `data/question_set.json`

### Steps

1. **Generate L1 + L2 (Python)** — Run `python3 phase1_generate_questions.py` to load the MMLU test split (2 questions per subject), apply L2 abbreviations, and save to `data/raw_questions.json`.
2. **Generate L3 (Claude Code Workflow)** — The `workflows/phase1_l3_generation.js` workflow loads `data/raw_questions.json` and spawns one Agent call per question with the L3 prompt. Results are collected and passed to Python to assemble and save `data/question_set.json`.
3. **Commit** — `data/question_set.json` is committed to the repo as a reproducibility artifact.

### question_set.json format

```json
[
  {
    "id": "high_school_mathematics_0",
    "subject": "high_school_mathematics",
    "correct_answer": "B",
    "choices": ["...", "...", "...", "..."],
    "l1": "What is the derivative of x^2?",
    "l2": "Wut is d derivative of x^2?",
    "l3": "Wut is d deriv of x^2?",
    "l3_generation_model": "claude-haiku-4-5-20251001"
  }
]
```

### L2 Lookup Table (`abbreviations.py`)

A dictionary mapping common words to their texting abbreviations. Examples:

```python
ABBREVIATIONS = {
    "you": "u",
    "are": "r",
    "what": "wut",
    "the": "d",
    "to": "2",
    "for": "4",
    "and": "n",
    "with": "w/",
    "because": "bc",
    "before": "b4",
    "please": "plz",
    "thanks": "thx",
    # ... etc.
}
```

Substitution is case-insensitive and whole-word only (does not match substrings).

### L3 Agent Prompt

Sent to one Agent per question during Phase 1 L3 generation:

```
System: "You are a text abbreviation engine. Your job is to shorten words in the input text where the abbreviated form would still be understandable to a human reader. Do not change any words that are already short (4 characters or fewer). Do not alter proper nouns, numbers, or answer choices. Return only the abbreviated text with no explanation."

User: [L2 question text]
```

---

## Phase 2: Evaluation Runner

**Triggered by:** Asking Claude Code to run the Phase 2 workflow with a model name argument.

### Model Selection

The evaluation model is passed as an argument to the workflow:

```
"Run the Phase 2 evaluation workflow with model claude-sonnet-4-6"
```

The model name is recorded in every row of the results CSV.

### Prompt Structure

Every one of the 342 Agent calls uses the exact same system prompt. The only difference between calls is the question text:

```
System: "You are a helpful assistant. Answer the following multiple choice question by responding with only the letter of the correct answer (A, B, C, or D)."

User: [question text at its level, including answer choices formatted as "A) ...\nB) ..."]
```

No conversation history. No prior context. Each call is a fresh, isolated Agent invocation.

### Concurrency

The workflow uses `pipeline()` over all 342 tasks. Claude Code's built-in concurrency cap (up to 16 simultaneous agent calls) acts as the natural rate limiter.

### Token Counting

Because Agent calls do not return a raw `usage` object, input tokens are **estimated** using character count:

```
estimated_input_tokens = max(1, (len(system_prompt) + len(user_message)) // 4)
```

Claude's average tokenization rate is approximately 1 token per 4 characters for English text. Output tokens are always 1 (a single letter: A, B, C, or D). This estimation is consistent across all 342 calls, so relative comparisons between L1/L2/L3 remain valid.

---

## Output Files

### raw_results_<model>.csv / raw_results_<model>.xlsx

One row per Agent call (342 rows). Columns:

| Column | Description |
|--------|-------------|
| `id` | Question ID (e.g. `high_school_mathematics_0`) |
| `subject` | MMLU subject |
| `level` | L1, L2, or L3 |
| `question` | The question text used in this call |
| `answer_given` | Model's response (A/B/C/D) |
| `correct_answer` | Ground truth |
| `is_correct` | Boolean |
| `input_tokens` | Estimated input tokens for this call (chars ÷ 4) |
| `output_tokens` | Always 1 (single letter response) |
| `total_tokens` | input_tokens + 1 |
| `model` | Model used |

### summary_report_<model>.md

Auto-generated after all calls complete. Sections:

1. **Study parameters** — model, sample size
2. **Accuracy by level** — L1 / L2 / L3 overall accuracy
3. **Accuracy by subject × level** — table of accuracy per subject at each level
4. **Token usage by level** — average estimated input tokens per call at each level
5. **Token savings** — % reduction in input tokens: L2 vs L1, L3 vs L1
6. **Key finding** — one-sentence summary of whether quality held while tokens dropped

---

## Extensibility

To run the study against a second model, ask Claude Code to run the Phase 2 workflow again with the new model name. Results write to `raw_results_<model>.csv` so outputs from different models don't overwrite each other.

---

## Key Constraints

- The system prompt must be **byte-for-byte identical** across all 342 evaluation calls — any difference outside the question text would introduce token variance that contaminates the comparison
- **Answer choices are never abbreviated** — only the question stem is transformed at L2 and L3. Choices remain in their original L1 form across all levels, ensuring the only variable is the question stem wording
- `question_set.json` must be committed before Phase 2 runs — it is the single source of truth for the study
- L3 variants are generated once in Phase 1 and never regenerated — this ensures L3 is consistent across all runs
- The model used for L3 generation in Phase 1 is recorded in `question_set.json` for reproducibility
- **LLM-calling phases (Phase 1 workflow, Phase 2 workflow) require an active Claude Code session** — they cannot be run as standalone Python scripts
- Token counts are **estimated** (chars ÷ 4), not exact. This is a known limitation; relative comparisons between levels remain valid because the estimation is applied identically to all calls
